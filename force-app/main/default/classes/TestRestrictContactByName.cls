
@isTest
private class TestRestrictContactByName {
    
    @isTest
    static void TestInsertUpdateContact(){
        Contact acc = new Contact(Name='TESTING');
        insert acc;

        Test.startTest();
            
            if()
    }
}